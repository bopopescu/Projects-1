while  True：
    if cpu利用率 >90%:
       #发送邮件提醒
       链接邮箱服务器
       发送油价
       关闭连接
    if 磁盘使用空间 >90%:
       #发送邮件提醒
       链接邮箱服务器
       发送油价
       关闭连接
    if 内存占用 >90%:
       #发送邮件提醒
       链接邮箱服务器
       发送油价
       关闭连接
代码重复过多  不符合高级程序员规范   后期维护难  还有一个致命问题
def   发送邮件服务器（内容）
       #发送邮件提醒
       链接邮箱服务器
       发送邮件
       关闭连接
 while  True：
    if cpu利用率 >90%:
        发送邮件（'cpu'告警）
    if 磁盘使用空间 >90%:
        发送邮件（'disk'告警）
    if 内存占用 >90%:
        发送邮件（'mem'告警）
定义：函数是将一组语句的集合通过一个名字（函数名）封装起来，要想执行这个函数，只需调用器函数名即可
def sayhi()#函数名
    print('hello')
sayhi()  #调用函数


a,b  =5,8
c =  a**b
print(c)
改成函数可以带参数
def  calc(x,y):
    res = x**y
    return res
c = calc(a,b)
print(c)



特性
  减少重复代码
  是程序变得可扩展
  是程序变得已维护
